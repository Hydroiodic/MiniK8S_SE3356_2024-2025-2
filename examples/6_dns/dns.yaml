kind: DNS
metadata:
  name: example-dns
  namespace: default
  labels:
    app: myapp
spec:
  host: internal-example.com
  paths:
    - path: /web
      serviceName: web-service
      servicePort: 8080
    - path: /
      serviceName: nginx-service
      servicePort: 80
